#include <stdio.h>

void main(void) {
    int x,y,cash,regester;
    double tax;

    x, y = 198, 138*2;
    cash = 1000;
    tax  = 1.1;
    regester = cash - (x + y) * tax;
    printf("%d円\n", regester);

}